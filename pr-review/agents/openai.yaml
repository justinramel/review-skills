interface:
  display_name: "PR Review"
  short_description: "Review a PR with a panel of parallel reviewers, reported as a verdict + confidence table"
  default_prompt: "Review the requested change with the parallel panel and enforce the provider-neutral high-reasoning model policy."
